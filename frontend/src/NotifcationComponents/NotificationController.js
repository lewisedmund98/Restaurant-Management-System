import React from 'react';

export default class NotifcationController extends React.Component {
    constructor(props) {
        super(props);
    }

    // do something to collect info on each order to be put into
    // a message to be used to notify waiters when specific orders
    // are ready for delivery
}